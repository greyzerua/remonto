# Архітектура проєкту Remonto

## 📐 Загальна структура

Проєкт побудований на **React Native** з **Expo** та використовує **Firebase** як backend. Архітектура слідує принципам **Clean Architecture** та **Separation of Concerns**.

```
remonto/
├── App.tsx                 # Точка входу, навігація та провайдери
├── config/                 # Конфігурація
│   └── firebase.ts        # Налаштування Firebase
├── contexts/              # React Context для глобального стану
│   ├── AuthContext.tsx    # Стан аутентифікації
│   └── FamilyGroupContext.tsx # Стан родинних груп
├── screens/               # UI екрани
│   ├── AuthScreen.tsx     # Вхід/Реєстрація
│   ├── FamilyGroupScreen.tsx # Вибір/створення групи
│   ├── ProjectsScreen.tsx # Список проектів
│   ├── ExpensesScreen.tsx # Список витрат
│   └── SettingsScreen.tsx # Налаштування
├── services/              # Бізнес-логіка та API
│   ├── auth.ts           # Аутентифікація
│   └── firestore.ts      # Робота з Firestore
├── types/                 # TypeScript типи
│   └── index.ts          # Всі типи даних
└── utils/                 # Допоміжні функції
    └── helpers.ts        # Утиліти (форматування, генерація ID)
```

---

## 🏗️ Шари архітектури

### 1. **Presentation Layer (UI)**
**Розташування:** `screens/`, `App.tsx`

**Відповідальність:**
- Відображення інтерфейсу користувача
- Обробка подій користувача
- Навігація між екранами

**Компоненти:**
- `App.tsx` - головний компонент з навігацією
- `AuthScreen` - екран входу/реєстрації
- `FamilyGroupScreen` - управління групами
- `ProjectsScreen` - список проектів з CRUD операціями
- `ExpensesScreen` - список витрат (поки placeholder)
- `SettingsScreen` - налаштування та профіль

**Особливості:**
- Використовує React Navigation для навігації
- SafeAreaView для коректного відображення на всіх пристроях
- Умовна навігація залежно від стану аутентифікації

---

### 2. **State Management Layer**
**Розташування:** `contexts/`

**Відповідальність:**
- Глобальний стан додатку
- Синхронізація стану між компонентами
- Управління сесією користувача

**Contexts:**

#### `AuthContext`
- Зберігає стан поточного користувача
- Надає функції: `user`, `authUser`, `userData`, `loading`
- Автоматично оновлюється при зміні стану аутентифікації

#### `FamilyGroupContext`
- Зберігає список груп користувача
- Зберігає поточну вибрану групу
- Надає функції: `groups`, `currentGroup`, `setCurrentGroup`, `refreshGroups`

**Переваги:**
- Централізоване управління станом
- Автоматичне оновлення UI при зміні даних
- Легке тестування та підтримка

---

### 3. **Business Logic Layer**
**Розташування:** `services/`

**Відповідальність:**
- Бізнес-логіка додатку
- Взаємодія з Firebase
- Валідація даних
- Трансформація даних

**Services:**

#### `auth.ts`
Функції аутентифікації:
- `registerUser()` - реєстрація нового користувача
- `loginUser()` - вхід користувача
- `logoutUser()` - вихід
- `getUserData()` - отримання даних користувача
- `onAuthStateChange()` - підписка на зміни стану

#### `firestore.ts`
Робота з Firestore (CRUD операції):

**Family Groups:**
- `createFamilyGroup()` - створення групи
- `getFamilyGroup()` - отримання групи
- `getUserFamilyGroups()` - групи користувача
- `addMemberToGroup()` - додавання учасника

**Projects:**
- `createProject()` - створення проекту
- `updateProject()` - оновлення проекту
- `deleteProject()` - видалення проекту
- `getProject()` - отримання проекту
- `getProjectsByGroup()` - проекти групи
- `subscribeToProjects()` - realtime підписка на проекти

**Expenses:**
- `createExpense()` - створення витрати
- `updateExpense()` - оновлення витрати
- `deleteExpense()` - видалення витрати
- `getExpense()` - отримання витрати
- `getExpensesByProject()` - витрати проекту
- `getExpensesByGroup()` - витрати групи
- `subscribeToExpenses()` - realtime підписка на витрати
- `subscribeToGroupExpenses()` - realtime підписка на витрати групи

**Особливості:**
- Realtime синхронізація через Firestore `onSnapshot`
- Автоматичне сортування на клієнті (щоб не потрібні індекси)
- Обробка помилок та валідація даних

---

### 4. **Data Layer**
**Розташування:** `config/firebase.ts`, `types/index.ts`

**Відповідальність:**
- Конфігурація підключення до Firebase
- Визначення типів даних
- Структура даних

**Компоненти:**

#### `config/firebase.ts`
- Ініціалізація Firebase App, Auth, Firestore
- Експортує готові екземпляри для використання

#### `types/index.ts`
TypeScript типи:
- `User` - користувач
- `FamilyGroup` - родинна група
- `Project` - проект ремонту
- `Expense` - витрата
- `ProjectStatus` - статуси проекту
- `ExpenseCategory` - категорії витрат
- `AuthUser`, `ProjectFormData`, `ExpenseFormData` - допоміжні типи

**Структура даних в Firestore:**

```
users/
  └── {userId}/
      ├── id, email, displayName, photoURL, createdAt

familyGroups/
  └── {groupId}/
      ├── id, name, ownerId, members[], createdAt, updatedAt

projects/
  └── {projectId}/
      ├── id, familyGroupId, name, description, status,
      ├── budget, startDate, endDate, createdBy, createdAt, updatedAt

expenses/
  └── {expenseId}/
      ├── id, projectId, familyGroupId, name, amount,
      ├── category, description, date, createdBy, createdAt, updatedAt
```

---

### 5. **Utilities Layer**
**Розташування:** `utils/helpers.ts`

**Відповідальність:**
- Допоміжні функції
- Форматування даних
- Генерація ID

**Функції:**
- `generateId()` - генерація унікальних ID
- `getCurrentDate()` - поточна дата в ISO форматі
- `formatDate()` - форматування дати
- `formatDateShort()` - коротке форматування дати
- `formatCurrency()` - форматування грошових сум (UAH)
- `getCategoryName()` - назва категорії витрат
- `getStatusName()` - назва статусу проекту

---

## 🔄 Потік даних

### Типовий сценарій (створення проекту):

```
1. Користувач натискає "Додати проект" (ProjectsScreen)
   ↓
2. Відкривається модальне вікно з формою
   ↓
3. Користувач заповнює форму та натискає "Зберегти"
   ↓
4. ProjectsScreen викликає createProject() з services/firestore.ts
   ↓
5. firestore.ts зберігає дані в Firestore
   ↓
6. Firestore автоматично оновлює всіх підписників через onSnapshot
   ↓
7. subscribeToProjects() отримує оновлені дані
   ↓
8. ProjectsScreen автоматично оновлює UI з новим проектом
```

### Realtime синхронізація:

```
Firestore Database
    ↓ (onSnapshot)
subscribeToProjects() / subscribeToExpenses()
    ↓
Callback з оновленими даними
    ↓
setProjects() / setExpenses()
    ↓
React re-render
    ↓
Оновлений UI
```

---

## 🔐 Безпека

### Аутентифікація:
- Firebase Authentication (Email/Password)
- Автоматична перевірка стану через `onAuthStateChanged`

### Правила Firestore:
- Тестові правила: дозволяють всім авторизованим користувачам читати/писати
- Для продакшену: обмеження доступу за участю в групах

---

## 📱 Навігація

### Умовна навігація:

```
App запускається
    ↓
Перевірка аутентифікації
    ↓
┌─────────────────┐
│ Не авторизований│ → AuthScreen
└─────────────────┘
    ↓
┌─────────────────┐
│ Немає групи     │ → FamilyGroupScreen
└─────────────────┘
    ↓
┌─────────────────┐
│ Все ОК          │ → Tab Navigator
└─────────────────┘
    ├── ProjectsScreen
    ├── ExpensesScreen
    └── SettingsScreen
```

---

## 🎯 Принципи архітектури

1. **Separation of Concerns** - кожен шар має свою відповідальність
2. **Single Responsibility** - кожен модуль робить одну річ
3. **DRY (Don't Repeat Yourself)** - повторюваний код винесено в утиліти
4. **Type Safety** - використання TypeScript для безпеки типів
5. **Realtime First** - синхронізація в реальному часі через Firestore
6. **Context API** - для глобального стану замість Redux (простіше для малих проєктів)

---

## 🚀 Масштабування

### Для розширення проєкту можна додати:

1. **Redux/MobX** - якщо стане багато глобального стану
2. **React Query** - для кешування та оптимізації запитів
3. **Formik/React Hook Form** - для складних форм
4. **React Native Paper** - готові UI компоненти
5. **Unit Tests** - Jest + React Native Testing Library
6. **E2E Tests** - Detox або Maestro

---

## 📦 Залежності

**Основні:**
- `react-native` - фреймворк
- `expo` - платформа розробки
- `firebase` - backend
- `@react-navigation/*` - навігація
- `react-native-safe-area-context` - безпечні зони

**Архітектурні рішення:**
- Context API замість Redux (простіше для початку)
- Прямі виклики сервісів замість middleware
- Realtime підписки замість polling

---

## 🔧 Налаштування

### Firebase:
- Конфігурація в `config/firebase.ts`
- Правила безпеки в Firebase Console
- Інструкції в `FIREBASE_SETUP.md`

### Environment:
- Змінні оточення через `EXPO_PUBLIC_*`
- Fallback значення в коді

---

## 📝 Висновок

Архітектура проєкту слідує сучасним практикам React Native розробки:
- ✅ Чіткий поділ на шари
- ✅ TypeScript для безпеки типів
- ✅ Realtime синхронізація
- ✅ Масштабована структура
- ✅ Легка підтримка та розширення

Проєкт готовий до додавання нових функцій та масштабування.

